@model MVC.Models.Tablero

@{
    ViewData["Title"] = "Delete";
}

<h1 class="text-center">Eliminar Tablero</h1>
<div class="d-flex justify-content-center mt-4">
    <div class="col-md-6">
        <div class="card shadow-sm">
            <div class="card-body">
                <h4 class="card-title text-center">¿Estás seguro que deseas eliminar este tablero?</h4>
                <hr />
                <dl class="row">
                    <dt class="col-sm-4">
                        @Html.DisplayNameFor(model => model.Titulo)
                    </dt>
                    <dd class="col-sm-8">
                        @Html.DisplayFor(model => model.Titulo)
                    </dd>
                    <dt class="col-sm-4">
                        @Html.DisplayNameFor(model => model.Subtitulo)
                    </dt>
                    <dd class="col-sm-8">
                        @Html.DisplayFor(model => model.Subtitulo)
                    </dd>
                </dl>

                <form asp-action="Delete" class="text-center">
                    <input type="hidden" asp-for="Id" />
                    <input type="submit" value="Eliminar" class="btn btn-danger" />
                    <a class="btn btn-secondary" asp-action="Index">Cancelar</a>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
